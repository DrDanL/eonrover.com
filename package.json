{
  "name": "eonrover",
  "private": true,
  "version": "0.1.0",
  "description": "Eon Rover - a browser-based multiplayer space strategy game",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
